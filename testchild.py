# Adding a new file to the child branch
# Basic Python code for making all letter in a name caps

def cap_name(name):
  caps=""
  for char in name:
    caps += char.upper()
  return cap_name
cap_name("joseph")
